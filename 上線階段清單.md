# Amplify 上線階段清單

把靜態站（文章寫在 `src/data/content.js`、GitHub Pages）改成 AWS 可後台更新，再切到 Amplify Hosting。

**硬性原則**

- 本站開**新的** Amplify app，不要接到既有預約系統 `clinic_appointment_system`（Cognito、DynamoDB `clinic_*` 都留給掛號）。
- 預約維持外連 `APPOINTMENT_URL`，不共用登入。
- 區域沿用 `ap-northeast-1`。
- `#/admin` 只是網址；真正權限在 API。
- **程式部署走 GitHub → Amplify 自動建置**：repo `dennis8989/juhaoweb` 接到 Amplify Hosting 後，指定分支有新的 push 就自動重新部署。不要再用 GitHub Actions 佈 GitHub Pages。

**Git 連動怎麼算「完成」**

| 事件 | 誰負責部署 |
|------|------------|
| `git push` 到已連接的分支（程式、樣式、後台 UI） | Amplify 自動 build `npm install` → `npm run build` → 上線 |
| 在 `#/admin` 新增／改文章 | 寫入 DynamoDB／S3，**不必**再 git、也不會觸發重新部署 |

兩件事不要混：改網站程式用 git；改衛教內容用後台。

標記：`必做` = 不完成不能進下一階段或不能切流量；`建議` = 不擋第一次上線，但應盡早做。

---

## Phase 0：AWS／Amplify CLI／分支

**目標：** 環境與安全邊界就緒，還沒改網站功能。

### 必做

- [x] 確認本機 AWS 憑證有效（`aws sts get-caller-identity`）
- [x] 安裝並登入 Amplify CLI
- [x] 開獨立 git 分支（例如 `amplify`），不要直接改 `main` 上正在跑的站
- [x] `amplify init` 建立**新的** juhaoweb backend／app
- [x] 確認沒有 init 進、也沒有關聯 `clinic_appointment_system`
- [x] Amplify Console 連接 GitHub repo `dennis8989/juhaoweb`（需你在瀏覽器授權 Amplify GitHub App，一次即可）
- [x] 先把自動部署接到 **非 main** 的工作分支（例如 `amplify`），產生預覽網址；此時 `main` 仍由 GitHub Pages 服務現況訪客

### 建議

- [x] 規劃後端環境：正式與測試**共用**現有 Gen 2 後端（跟著 `amplify` 分支）；不另開 production 庫
- [ ] 開啟 Amplify 分支預覽（PR 自動長預覽網址）。目前改用獨立 `dev` 分支測試，PR preview 仍未開

**完成標準：** 本機能對新 app 下 Amplify 指令；訪客看到的內容不變。

### Phase 0 紀錄

| 項目 | 值 |
|------|-----|
| Amplify app | `juhaoweb`（`d1zwf54820xsiz`） |
| 區域 | `ap-northeast-1` |
| GitHub | `dennis8989/juhaoweb`；Amplify 連 `amplify`（正式）與 `dev`（測試） |
| 正式網址（Amplify） | https://amplify.d1zwf54820xsiz.amplifyapp.com |
| 測試網址 | https://dev.d1zwf54820xsiz.amplifyapp.com |
| Console | https://ap-northeast-1.console.aws.amazon.com/amplify/apps/d1zwf54820xsiz |
| 後端 | Amplify Gen 2，跟著 `amplify` 分支（stack `amplify-d1zwf54820xsiz-amplify-branch-ce4443571c`）；`dev` 只 generate outputs，不另建後端 |
| git `main` | 未接 Amplify；GitHub Pages 仍服務現況訪客 |
| 預約系統 | 仍是獨立 app `d29yyigp5o4a8q`，未共用 |

---

## Phase 1：Cognito + DynamoDB Article + S3 + API

**目標：** 後端三件套與授權就緒，前台畫面仍讀本機資料。

### 必做

- [x] Cognito User Pool（後台登入）
- [x] DynamoDB 表 `Article`
- [x] S3 bucket（文章圖片）
- [x] API（AppSync GraphQL 或 REST）
- [x] 授權規則：未登入可 `list`／`get`**已上架**文章；`create`／`update`／`delete`／上傳僅限已登入
- [x] 關閉 Cognito 公開註冊
- [x] Article 欄位對齊現站，至少包含：
  - `id`
  - `title`
  - `excerpt`
  - `content`（段落陣列）
  - `facebookUrl`
  - `dirs`
  - `subs`
  - `articleCats`
  - `images`
  - `status`（`published`＝上架／`draft`＝下架）
  - `createdAt`
- [x] S3 文章圖：bucket 維持 Block Public Access；guest／登入可用 Amplify `getUrl`（Phase 2 接前台）

### 建議

- [x] 開啟 DynamoDB PITR
- [x] 開啟 S3 versioning
- [x] 用 Console 或 CLI 建立第一個管理員帳號（可延到 Phase 4，但不要做成開放註冊頁）

**完成標準：** 雲端有身分、文章表、圖檔空間與 API；公開網站仍讀 `content.js`。

### Phase 1 紀錄

| 項目 | 值 |
|------|-----|
| 後端 | Amplify Gen 2（`defineAuth` / `defineData` / `defineStorage`） |
| User Pool | `ap-northeast-1_B0PsM79TS`（email 登入；公開註冊已關） |
| 管理員 | `dennishuang0809@gmail.com`（首次登入須改密碼） |
| GraphQL | `https://myggba2ge5crngelrdlbmvmz6e.appsync-api.ap-northeast-1.amazonaws.com/graphql` |
| DynamoDB | `Article-bpfnwne3pre6zeeur5bmzjkuka-NONE`（PITR on） |
| S3 | `amplify-d1zwf54820xsiz-am-articleimagesbucket9abb8-ba4spjveugpe`（versioning on、Block Public Access） |
| 公開讀 | 自訂 query `listPublishedArticles`／`getPublishedArticle`（API Key，僅 `published`）；寫入需 Cognito |
| 前台 | 已改讀 API（Phase 2） |

---

## Phase 2：articlesRepository；公開站改 API

**目標：** 前台不再直接讀 `articles` 陣列，改走同一套資料層。

### 必做

- [x] 新增 `articlesRepository`：列表、依 id 取一篇、依分類篩選
- [x] `#/articles/...`、`#/article/...` 與主題頁相關文章都改打 repository
- [x] repository 內部呼叫 Amplify API（讀 DynamoDB）
- [x] 圖片路徑：把 S3 key 轉成可顯示網址（不可再寫死 `/articles/*.jpg` 當唯一來源）
- [x] 只顯示 `status = 上架` 的文章
- [x] 沿用現有 hash 路由（`#/article/...`）

### 建議

- [x] 載入中／失敗／空列表狀態，避免 API 掛掉時整頁空白

**完成標準：** 訪客看到的文章來自雲端。此時還沒後台也能讀，只是不能在瀏覽器新增。

雲端文章表已在 Phase 3 seed 28 篇；公開站改讀 API 後應與現況內容對得上。

---

## Phase 3：seed 既有約 28 篇

**目標：** 把現有內容灌進 DynamoDB／S3，避免上線後列表變空。

### 必做

- [x] 從 `content.js` 匯出約 28 篇（標題、摘要、內文、分類、Facebook 連結、status=上架）
- [x] 上傳 `public/articles/` 約 33 張圖到 S3，並寫回各篇 `images`
- [x] 跑一次 seed（打預覽後端，不要誤打正式庫——若 Phase 0 已分環境）
- [x] 核對：列表、各 `articleCats`／主題頁、單篇內文、配圖、Facebook 連結都與現站一致

**完成標準：** 雲端資料 = 現在網站內容。Phase 2 的公開站與現況對得上。

### Phase 3 紀錄

| 項目 | 值 |
|------|-----|
| 文章 | 28 篇（`status=published`，id 與現站 hash 相同） |
| 圖片 | 33 張 → `s3://…/public/articles/` |
| 指令 | `npm run seed:articles`（只打 juhaoweb 預覽 bucket／Article 表） |
| 預覽 | https://amplify.d1zwf54820xsiz.amplifyapp.com |

---

## Phase 4：`#/admin` 最小後台

**目標：** 登入後可新增／編輯／刪除文章；不做完整 CMS。

### 必做

- [x] 路由 `#/admin`（沿用 hash）
- [x] Cognito 登入；未登入不能進後台、也不能打寫入 API
- [x] 文章列表
- [x] 新增、編輯、刪除
- [x] 多圖上傳到 S3
- [x] 分類選擇：對應現有 `articleMenu`／主題頁（`dirs`、`subs`、`articleCats`）
- [x] 上架／下架開關
- [x] 存檔後公開站能讀到新內容（僅上架者）
- [x] 建立第一個管理員（若 Phase 1 還沒做）；**不要**做開放註冊

### 建議

- [x] 忘記密碼流程（Cognito 內建即可）
- [x] 刪除前確認；避免誤刪 seed 文

**完成標準：** 你能在瀏覽器新增一篇、選分類、上傳圖、上架，公開站對應分類看得到。訪客不能新增。

### Phase 4 紀錄

| 項目 | 值 |
|------|-----|
| 後台 | `#/admin`（未登入只看得到登入表單） |
| 管理員 | `dennishuang0809@gmail.com` |
| 公開註冊 | 關閉 |

---

## Phase 5：Amplify Hosting 上線（Git push 自動部署）

**目標：** 正式站改由 Amplify 看 GitHub（正式線為 `amplify`，測試線為 `dev`，共用現有後端）。切離 GitHub Pages，訪客只走一個正式站。

### 必做

- [x] 確認 Hosting 是 **juhaoweb 這顆新 app**（`d1zwf54820xsiz`），不是預約站 `clinic_appointment_system`
- [x] 正式線維持 git／Amplify 的 **`amplify`**（不當 git `main`）；另開 **`dev`** 測試，兩站共用現有後端
- [x] 建置設定：`npm install`、`npm run build`、產物目錄 `dist`（Gen 2 套件改用 `npm install`，不用 `npm ci`）
- [x] Amplify 建置沒有 `GITHUB_ACTIONS`，`base` 為 `/`；僅 GitHub Pages 才用 `/juhaoweb/`
- [x] 接上 Phase 1 的 Cognito／DynamoDB／S3／API（`amplify` 跑 `pipeline-deploy`；`dev` 只 `generate outputs --branch amplify`）
- [x] 驗證：`git push origin amplify` → Amplify Console 自動部署成功；`dev` 第一次建置亦成功
- [x] 公開站與 `#/admin` 在 Amplify 網址都能用（正式 https://amplify.d1zwf54820xsiz.amplifyapp.com 、測試 https://dev.d1zwf54820xsiz.amplifyapp.com ）
- [x] **關掉** `.github/workflows/deploy.yml`（已從 `main`／`amplify`／`dev` 刪除，之後 push 不會再佈 Pages）
- [ ] 在 GitHub **Settings → Pages** 把 Source 設成 **None** 並 Save（Actions 無權關閉已上線的 Pages；舊網址可能還看得到，直到這裡關掉）

### 建議

- [ ] 自訂網域與 HTTPS（可第二步；先用 `*.amplifyapp.com` 也可以）
- [ ] 確認 `og-image.png`（現站引用但檔案不存在，社交分享圖會缺）
- [ ] 切流後抽查：首頁、一篇舊文、一篇後台新文、分類頁、預約外連、圖片

**完成標準：** `git push origin amplify` → Amplify 自動部署成功。訪客看雲端文章；改衛教用 `#/admin`，不必為了一篇文章再 push。只有一個正式站（GitHub Pages 關閉後即達標）。

### Phase 5 紀錄

| 項目 | 值 |
|------|-----|
| Hosting | `juhaoweb`（`d1zwf54820xsiz`） |
| 正式分支 | `amplify` |
| 測試分支 | `dev`（共用 amplify 後端） |
| 未接 Amplify | git `main`（Pages 自動部署已停；網站需在 Settings 關掉） |
| Console 注意 | 不要刪 Amplify 上的 `amplify` 分支，後端 stack 掛在那裡 |

---

## 這次不做（刻意排除）

以下不是 CMS 流程漏洞，上線後行為會跟現在一樣，若要做另開階段：

- 關於我、門診時間、診所地址、真實案例頁仍在 `content.js`，改這些還是要改程式再部署
- 合作邀約表單目前按「發送郵件」不會寄出（只有 `preventDefault`）
- 預約掛號維持連到 `clinic_appointment_system`

---

## 建議的階段依賴

```
0 新 app + 連接 GitHub（先自動部署工作分支，預覽網址）
→ 1 資料＋圖＋API 授權＋文章欄位
→ 2 前台改讀 API＋圖片網址
→ 3 seed 舊文舊圖
→ 4 後台（含分類與上架）
→ 5 正式線看 amplify、測試線看 dev（共用後端）、驗證 push 自動部署、停 Pages
```
